<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.example</groupId>
    <artifactId>servlet-assignment</artifactId>
    <version>1.0</version>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <tomcat.version>10.1.34</tomcat.version>
    </properties>

    <dependencies>
        <!--
          jakarta.servlet-api (the interfaces we code against) + embedded Tomcat
          (the servlet CONTAINER that runs them), in one artifact.
        -->
        <dependency>
            <groupId>org.apache.tomcat.embed</groupId>
            <artifactId>tomcat-embed-core</artifactId>
            <version>${tomcat.version}</version>
        </dependency>

        <!--
          Needed only because we start Tomcat with addWebapp() rather than addContext().

          addWebapp() is what runs the ANNOTATION SCANNER, which is how @WebServlet and
          @WebFilter get discovered - that is the whole reason we use it. But addWebapp()
          also wires up Tomcat's default web.xml, which registers the JSP servlet with
          load-on-startup. If Jasper (the JSP engine) is not on the classpath, that
          registration fails and the whole context refuses to start.

          We never write a JSP. This dependency exists purely so the default web.xml
          can be satisfied.
        -->
        <dependency>
            <groupId>org.apache.tomcat.embed</groupId>
            <artifactId>tomcat-embed-jasper</artifactId>
            <version>${tomcat.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Pinned explicitly: Maven 3.8's bundled compiler plugin (3.1) is too old
                 to understand <maven.compiler.release> and defaults to Java 5. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.1.0</version>
                <configuration>
                    <mainClass>com.example.Main</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
